<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Traffico in chiaro consentito SOLO verso gli host di sviluppo locale.
      Per il resto (base-config) il cleartext è vietato: in produzione il
      backend deve essere HTTPS.

      Chi testa contro un backend/Keycloak sulla propria rete locale aggiunge
      qui l'IP LAN (es. quello in local.properties: BACKEND_IP / KEYCLOAK_IP).
      Questo blocco <domain-config> va rimosso prima di un rilascio reale.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">192.168.1.54</domain>
        <domain includeSubdomains="false">192.168.1.155</domain>
        <domain includeSubdomains="false">172.20.10.3</domain>
    </domain-config>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
